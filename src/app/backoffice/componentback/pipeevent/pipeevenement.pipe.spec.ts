import { PipeevenementPipe } from './pipeevenement.pipe';

describe('PipeevenementPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeevenementPipe();
    expect(pipe).toBeTruthy();
  });
});
